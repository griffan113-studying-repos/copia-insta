import {StyleSheet} from 'react-native';

export const HeaderStyles = StyleSheet.create({
    avatar: {
        height: 50,
        width: 50,
        margin: 10,
        borderRadius: 30
    },
    header: {
        flexDirection: "row",
        alignItems: "center",
    }
})